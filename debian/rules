#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_configure:
	mkdir build
	cd build && cmake ../ -DCMAKE_INSTALL_PREFIX=/usr

override_dh_auto_build:
	cd build && cmake --build . -- -j"$$(nproc)"

override_dh_auto_install:
	cd build && make install DESTDIR="$(CURDIR)/debian/azahar"